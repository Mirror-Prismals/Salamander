Drop soundtrack WAV files in this folder.

The `SoundtrackSystem` will randomly choose one file, play it once, then wait a random
gap (configured in `BaseSystem/registry.json`) before starting the next track.
